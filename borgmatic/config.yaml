location:
    source_directories:
        - /mnt/source
    repositories:
        - /backups/reddarr

storage:
    encryption_passphrase: "changeme"

retention:
    keep_daily: 7
    keep_weekly: 4
    keep_monthly: 6

consistency:
    checks:
        - repository
        - archives
